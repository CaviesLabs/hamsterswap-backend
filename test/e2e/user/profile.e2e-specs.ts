export function shouldGetUserProfileSucceed(this: Mocha.Context) {
  // TODO: not done
}

describe('[User] profile', function () {
  it('Should get user profile succeed', shouldGetUserProfileSucceed);
});
